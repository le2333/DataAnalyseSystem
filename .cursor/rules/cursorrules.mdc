---
description: 
globs: 
alwaysApply: false
---
背景：
这个项目的目标是开发一个能够进行可视化时间序列分析的本地webapp。

架构与技术栈：
前端采用electron和react框架，ui采用material ui。主界面用react flow搭建交互式流程图，在图中的节点中嵌入控件、状态显示和bokeh图表。

后端采用通过prefect驱动的工作流架构。所有方法均用节点包装。采用pandas.df存储时间序列。针对不同数据格式开发对应的端口，数据均采用列表储存，端口集成连接可行性检查。工作流配置持久化采用json格式。

前后端通信采用flask框架，通过websocket传输事件。

要求：
分析问题时*必须*查找根本原因，并明确引用相应的代码或文件，*不允许*推测。
生成代码时*必须*采用最简实现，*避免*修改或添加用户未明确指出的功能。
及时清除不再需要的代码。